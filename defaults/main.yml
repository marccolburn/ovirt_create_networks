---
vm_networks:
  - name: default1
    vlan: 999
    datacenter: default
    clusters:
      - name: Default
        assigned: True

ovirt_hosts:
  - name: host1
    interface: eth1
    networks:
      - name: vlan10
      - name: vlan20
